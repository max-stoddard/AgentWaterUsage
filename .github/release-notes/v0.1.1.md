# Agentic Insights 0.1.1

This patch release fixes the published CLI manifest after the initial `0.1.0` npm release.

- fixes the published CLI manifest so npm keeps the `agentic-insights` launcher
- keeps the release workflow aligned with the corrected package metadata
